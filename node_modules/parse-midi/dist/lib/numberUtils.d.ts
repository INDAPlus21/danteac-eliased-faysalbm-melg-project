/**
 * Combine "Most Significant Byte" and "Least Significant Byte" for
 * parameters that use 2 bytes instead of just 1 for increased resolution.
 */
export declare const combineMsbAndLsb: (msb: number, lsb: number) => number;
